/**
 * opponentBuildDrop handler
 * Placeholder - SmartRouter routes this to captureOpponent, startBuildCapture, etc.
 */
function opponentBuildDrop(state, payload, playerIndex) {
  return state;
}
module.exports = opponentBuildDrop;