/**
 * SmartRouter (Shared) - DEPRECATED
 * 
 * This module is kept for backward compatibility.
 * Use Router.js directly instead.
 * 
 * @deprecated Use require('./Router') instead
 */

const Router = require('./Router');

// Re-export Router as SmartRouter for backward compatibility
module.exports = Router;
