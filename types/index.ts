/**
 * types/index.ts
 * Barrel export for all shared types.
 * 
 * Import types from here for cleaner imports across the codebase.
 * Example: import { Card, GameState } from '../types';
 */

// Card types
export * from './card.types';

// Game types
export * from './game.types';
